@model IEnumerable<CRM.Domain.Entities.CategReclam>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReclamCateg)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReclamCateg)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategReclamId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CategReclamId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CategReclamId })
        </td>
    </tr>
}

</table>
