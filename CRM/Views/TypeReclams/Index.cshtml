@model IEnumerable<CRM.Domain.Entities.TypeReclam>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReclamType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReclamType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TypeReclamId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TypeReclamId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TypeReclamId })
        </td>
    </tr>
}

</table>
